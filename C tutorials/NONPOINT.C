#include<stdio.h>
#include<conio.h>


       //count of words by using pointer( array is not used)

/*void main()
{
  char  *a ;
  int count=0,i;
   clrscr();
   printf("Enter the line:\n");
   gets(a);
 while(*a!='\0')
  {
   a++;
   count++;
   }

  printf("=%d",count);

  getch();}  */


  //count of words with pointer

/*  void main()
  { char *a;
  int count=0;
  clrscr();
  printf("ENter the words:\n");
  gets(a);
  while(*a!='\0')
  { if(*a == ' ')
   count++;
   a++;

   }
   count++;
   printf("%d",count);
   getch();}*/


    //present of alphabet with pointer

   /*void main()
  { char *a;
    int i,b=0;
    clrscr();
    printf("Enter the string:\n");
    gets(a);
   for(i=0;*a!='\0';i++)
   { a++;
    // if(i == 0)
     //{printf("not present");}

     if(*a == 'o')
     {b++;
     printf("%d",b);
     }}

     if(b == 0)
     printf("not");
    // printf("%d",i);

     //if(i==0)
    // printf("not present");

    // printf("=%d",i);

     getch();}  */
